const autoprefixer = require("autoprefixer");

module.exports = {
  // plugins: [require("autoprefixer")],
  plugins: [require("postcss-preset-env")],
};

/* "browserslist": [
    "last 4 versions, not dead, > 2.2%",
    "ie > 9"
  ], */
